#include "main.h"
/**
 * mul - function that multiples two integers
 *
 * @a : first function parameter
 * @b : second function parameter
 * Return: 0 always
 */
int mul(int a, int b)
{
	return (a * b);
}
